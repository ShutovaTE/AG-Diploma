<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    android:fillViewport="true"
    android:background="@color/background_primary">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- Progress Bar -->
        <ProgressBar
            android:id="@+id/progressBar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_marginBottom="16dp"
            android:visibility="gone" />

        <!-- Title -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Название выставки"
            android:textSize="16sp"
            android:textStyle="bold"
            android:layout_marginBottom="4dp" />

        <EditText
            android:id="@+id/etTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="16dp"
            android:hint="Введите название выставки"
            android:background="@drawable/edit_text_background"
            android:padding="12dp" />

        <!-- Description -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Описание"
            android:textSize="16sp"
            android:textStyle="bold"
            android:layout_marginBottom="4dp" />

        <EditText
            android:id="@+id/etDescription"
            android:layout_width="match_parent"
            android:layout_height="120dp"
            android:layout_marginBottom="16dp"
            android:gravity="top"
            android:hint="Введите описание выставки"
            android:inputType="textMultiLine"
            android:maxLines="5"
            android:background="@drawable/edit_text_background"
            android:padding="12dp" />

        <!-- Author Only Checkbox -->
        <CheckBox
            android:id="@+id/cbAuthorOnly"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="24dp"
            android:text="Приватная (другие пользователи не смогут добавлять работы, но выставка будет видна всем)"
            android:textSize="14sp" />

        <!-- Create Button -->
        <Button
            android:id="@+id/btnCreateExhibition"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Создать выставку"
            android:textSize="16sp"
            android:textStyle="bold"
            android:backgroundTint="@color/accent_primary"
            android:textColor="@color/surface_primary" />

    </LinearLayout>
</ScrollView>
